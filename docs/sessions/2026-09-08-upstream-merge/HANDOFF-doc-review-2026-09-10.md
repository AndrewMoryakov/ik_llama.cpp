# Передача: ревью документации, 2026-09-10

От сессии, которая в тот же день провела слияние с upstream, прошла рунбук Step0
и делала проверку актуальности документации. Адресовано сессии, выпустившей
`evidence-12` … `evidence-16`.

Оси ревью разные и не пересекаются, кроме одного пункта. Ваше ревью читало
исходники, это читало документы.

## 1. Метод и охват

Два агента прошли 307 файлов `.md` из `dev`, кроме `github-data/` (626 файлов
чужих обсуждений). Разделение: `project_docs/` 133 файла, остальное 174.

Критерий отбора задавался явно, потому что он здесь главное. **Датированная
запись о прошлом устареванием не считается.** Февральский отчёт вправе описывать
февраль. Докладывать велено только три случая: утверждение подано как текущее без
даты и оно ложно; документ служит точкой входа, планом или указателем и поведёт
читателя не туда сегодня; упоминается путь, ветка или артефакт, которых нет,
причём так, что читатель попробует по ним пойти.

Из 307 файлов ~101 признан законной исторической записью и не докладывался.

Найденное выборочно сверено с самими файлами вручную. Всё ниже проверено, кроме
случаев, отмеченных особо.

## 2. Чего нет в плане evidence-15

Ваш пункт 5 говорит: «В документации есть устаревшие Raptor handoff, путь Vulkan
smoke, неполный build-gate, прежний список разрешённых тестовых отказов и
противоречивый статус Step0». Ниже конкретика, которой там нет.

### 2.1. `README.md`: битая единственная точка входа

Строка 323 вела на `docs/superpowers/specs/00-INDEX.md`. **Такого файла нет.**
Индекс переехал в `docs/sessions/`, ссылку не обновили. Это был единственный вход
из README во весь набор документов по ускорению MoE.

Строки 104–107 объявляли сессию `2026-09-07` текущей работой, со словами
work-in-progress и с названием модели M2.7. Сессия `2026-09-08` из README видна
не была.

**Исправлено 2026-09-10**, коммит `28f9d5a0`.

### 2.2. `project_docs/llm/`: точка входа для агентов ведёт в март

`SOURCE_OF_TRUTH_MAP.md:7` помечает `PROJECT_STATE_FOR_AGENTS_2026-03-06.md`
как **CURRENT**, `:23` то же для `BASELINE_2026-03-06.md`.
`SESSION_BOOTSTRAP.md:9` велит начинать с `2026-03-06`, а `llm/README.md:13`
с `2026-03-04`, который два других файла считают превзойдённым.

Все три недатированы. Ни один не упоминает `rtr-auto/`, `hardware/` и ничего
после марта. Агент, стартующий отсюда, оказывается на полгода позади и не узнаёт
ни что rtr-auto отгружен, ни что Step0 состоялся.

### 2.3. `project_docs/research/weighted-hot-experts/`: план против опровергнутой гипотезы

`BENCHMARK_PLAN.md:5-6` задаёт модель `MiniMax M2.5 UD-Q5_K_XL, 151GB` по пути
`D:\ggufs\un\minimax2.5-m2\`, `:34-57` разворачивают матрицу из девяти прогонов.
`PROBLEM_AND_HYPOTHESIS.md:6` повторяет «151GB модель, 96GB RAM, ~55GB в swap».

Оба недатированы. Путь пуст, артефакта нет. Проверяемая гипотеза, что лучшее
взвешивание на стороне промпта даст лучшее горячее множество, опровергнута
измерением: нормализованная энтропия выбора 0.919, малого стабильного ядра нет
(`evidence-8` §4).

`research/expert-selection/README.md:28-32` называет этот план «first concrete
implementation target».

### 2.4. `project_docs/hardware/`: три файла открываются мёртвой веткой

`QUICKSTART_RAPTOR_LAKE.md:5` и `QUICKSTART_RAPTOR_LAKE_RU.md:5`:
`Build branch: feature/raptor-lake-laptop`. `LAPTOP_BENCH_PROTOCOL.md:19`:
`git checkout feature/raptor-lake-laptop` как шаг 0 процедуры.

Ветка переименована в `exp/raptor-runtime`. Первая же команда падает.

`hardware/AGENT_BRIEF.md:34` и `:177` утверждают, что политика `-rtr auto` v3
«shipped in PR #1738» — PR закрыт, upstream функцию не взял. `:74-78` подают
устаревший алиас `_mm256_dpbusd_epi32` как готовое исправление. `:260-264`
велят отложить предложение `GGML_AVX_VNNI` до разрешения #1738, тогда как PR
#2430 на эту тему уже открыт.

### 2.5. `docs/parameters.md`: ложный принцип сборки

Строки 497–499: «In general, use as few build flags as possible. The building
process automatically detects the available hardware features and enables them».
Дальше `cmake -B build -DGGML_NATIVE=ON`.

**Это ровно то утверждение, которое на MSVC ложно.** Единственная оставшаяся в
репозитории команда сборки без флагов `GGML_AVX512*`, и в отличие от README
рядом нет ссылки на `docs/build.md`.

`docs/build.md` проверен и **верен**: раздел про флаги AVX-512 даёт все четыре,
объясняет гейт `HAVE_FANCY_SIMD`, механику MSVC и проверку по строке
`HAVE_FANCY_SIMD is defined`.

### 2.6. `docs/superpowers/specs/analysis-1-prefetch-design.md`

Недатированный инженерный проект на полтора дня для префетча, который Step0
отверг измерением. Файл сам себя ограничивает на строке 12 («worth implementing
only if Step0 and trace show enough physical misses»), но ответ гейта в нём не
записан. Соседний `analysis-3` получил шапку **STATUS: HISTORICAL ANALYSIS**,
этот не получил.

### 2.7. `AGENTS.md`: чего не покрыла вчерашняя шапка

Строки 147–155 «Current Best Next Work» велят «return to MiniMax only with a new
smarter locality hypothesis or a dedicated large benchmark window». MiniMax M2.5
Step0 и есть текущая магистраль. Строка 71 называет ветку
`feature/raptor-lake-laptop`. Строки 44–46 говорят о «preassembled Path A patch
на ветке `pr/rtr-auto-mode-v2`, ready to force-push» — ветка теперь тег-архив.

## 3. Единственное пересечение: разбор логов Step0

Ваш `evidence-15` пункт 4: «Step0 parser выбирает последнюю совпавшую строку и
требует fixture для `runs/tokens`». Дефект тот же, механику уточню, потому что
она объясняет, почему `1a` падает **всегда**, а не иногда.

Проверено:

- `step0-bench.ps1:303` — регулярное выражение принимает и `runs`, и `tokens`;
- в логе прогона **ноль** вхождений `llama_print_timings`. Строки со словом
  `runs` не существует вовсе;
- причина: upstream коммитом `0a415bde` (2026-07-06) добавил в `main.cpp`
  собственную печать итогов и сделал вызов `llama_print_timings` условным —
  `if (n_decoded > 0) { ... } else { llama_print_timings(ctx); }`. При нормальной
  генерации ветка `else` недостижима;
- поэтому разбирается строка `main:`, печатающая `n_decoded` = 512;
- сайдкар сверяется с `ctx->n_eval` = 511. Разница структурная: первый выданный
  токен выходит из батча промпта, который увеличивает `n_p_eval`, а не `n_eval`;
- `step0-bench.ps1:863` при этом сам вычисляет `$expectedEvalRuns = NGen - 1` =
  511, то есть верное значение у него есть, но в проверку сайдкара уходит
  разобранное 512.

Отсюда следует, что «первая или последняя совпавшая строка» роли не играет:
подходящих строк одного вида. Чинить надо различение источника, а не порядок
выбора.

**Важное последствие для плана.** `$evalRuns` используется не только в проверке
сайдкара, но и в расчёте установившегося окна (`Get-SteadyMetric`, вызов на
`:334`). Правка сдвинет производные метрики уже снятой базовой линии примерно на
0.2%. Это не блокирует, но требует пометки в `evidence-7`, иначе числа до и после
станут несопоставимыми молча.

Правку не делали: решение владельца не получено.

## 4. Что уже исправлено сегодня, переделывать не нужно

- `FORK_WORKFLOW.md`, коммит `0bb93114`: раздел миграции помечен выполненным с
  фактическим итогом и тремя отклонениями от плана; **гейт сборки дополнен
  флагами AVX-512** (он предписывал сборку без них); раздел PR заменён полным
  итогом по одиннадцати PR; отмечено, что `feature/minimax-step0-readiness`
  подлежит удалению.
- `AGENTS.md`, тот же коммит: снято ложное «PR #1738 is still open», добавлен
  пункт про инструкции сборки без `GGML_AVX512_VNNI`.
- `README.md`, коммит `28f9d5a0`: см. §2.1.
- Баннеры STOP в `NEXT_AGENT_PROMPT.md` и `project_docs/rtr-auto/AGENT_BRIEF.md`
  — они попали в ваши коммиты `4ddfa58f` и `2453c28d`, см. §5.
- `00-INDEX.md` сессии переписан под все документы каталога.

## 5. Общее рабочее дерево: предупреждение

Две сессии работали в одном каталоге `ik_llama_my-fork/upstream-integration`
одновременно. Мои правки баннеров лежали несохранёнными, когда ваш `git add`
их подобрал: коммиты `4ddfa58f` и `2453c28d` содержат мой дословный текст под
вашими сообщениями. Ничего не потеряно, но границы коммитов и авторство
смешались.

Git не изолирует сессии внутри одного рабочего дерева ничем: `git add -A`
забирает всё, что там лежит. Разделяют не ветки, а каталоги — `git worktree add`
даёт каждой сессии свой индекс при общей истории.

## 6. Границы этой проверки

**Не проверялось:** файлы не `.md`; `docs/branch-manifest-2026-09-08.tsv` и
`docs/rtr-handoff/*.log` (выгрузка содержала только Markdown, их отсутствие в
ней ничего не означает); внутренняя согласованность `project_docs/models/`
и `runbooks/`, где найдено противоречие по `-t 8` против `SPEED_OPTIMIZATION.md`,
предшествующее всему сегодняшнему.

**Оставлено намеренно:** ~101 файл с датами в имени или заголовке, описывающие
своё время честно. `rtr-auto/ANALYSIS.md` образцовый случай: недатированное имя,
но явная шапка «Historical snapshot» с перечнем собственных превзойдённых
допущений.

**Замечено, не проверено:** `docs/sessions/2026-09-07-upstream-snapshot/00-INDEX.md`
строки 27–43 перечисляют двенадцать файлов по путям, которых в том каталоге нет —
они лежат в `docs/superpowers/specs/`. Разрешаются только `analysis-8..11`.

---

## 7. Дополнение 2026-09-10: открытый пункт §6 проверен

В §6 индекс сессии `2026-09-07` был помечен как «замечено, не проверено».
Проверено. Разрешение всех ссылок посчитано скриптом по выгрузке дерева `dev`.

**Из двадцати ссылок битых пятнадцать, разрешается пять.**

Разрешаются: `analysis-8`, `analysis-9`, `analysis-10`, `analysis-11` и
`../2026-09-08-upstream-merge/00-INDEX.md`.

Битые тринадцать указывают на файлы, фактически лежащие в
`docs/superpowers/specs/`:

```
2025-moe-ssd-inference-speedup.md
analysis-1-prefetch-design.md
analysis-2-bytes-per-token.md
analysis-3-step0-measurements.md
analysis-4-adjacent-kv-cpu.md
analysis-5-creative-disk-bound.md
analysis-6-fork-branch-opportunities.md
analysis-7-branch-solution-projection.md
step0/MINIMAX_METRICS_IMPLEMENTATION_REVIEW_2026-07-22.md
step0/MINIMAX_METRICS_PACKAGE_SPEC_2026-07-22.md
step0/MINIMAX_READINESS_REVIEW_2026-07-21.md
step0/MINIMAX_TARGET_RUNBOOK.md
step0/NEXT_AGENT_PROMPT.md
```

Индекс лежит в `docs/sessions/2026-09-07-upstream-snapshot/`, а писался так,
будто находится в `docs/superpowers/specs/`. Починка для этих тринадцати
механическая: префикс `../../superpowers/specs/`.

Оставшиеся две ссылки требуют своих путей: `FORK_WORKFLOW.md` лежит в корне
репозитория (`../../../FORK_WORKFLOW.md`), `tools/moe_cache_sim/README.md`
тоже считается от корня (`../../../tools/moe_cache_sim/README.md`).

**Оговорка о методе.** Поиск фактического расположения шёл по имени файла, и
для `README.md` это дало ложное совпадение с `README.md` в корне выгрузки.
Настоящий адресат `tools/moe_cache_sim/README.md`. Для остальных четырнадцати
имена уникальны, ложных совпадений быть не могло.

Само по себе это ломает не только навигацию. `analysis-3` несёт шапку
**STATUS: HISTORICAL ANALYSIS**, а `analysis-1` (проект префетча, отвергнутого
Step0) такой шапки не имеет — и оба недостижимы из индекса, который на них
ссылается. Читатель, дошедший до них другим путём, увидит только один из двух
предупреждающих знаков.

Правку не делал намеренно: окончательное исправление документации остаётся за
сессией, ведущей `evidence-12` … `evidence-16`, чтобы не столкнуться в общем
рабочем дереве второй раз.

---

## 8. Закрытие передачи 2026-09-10

Документационные правки выполнены в `dev` после передачи:

- `d7187273`: исправлены все двадцать локальных ссылок snapshot-индекса;
  повторная проверка разрешила 20 из 20, битых ссылок нет. Prefetch-дизайн
  получил историческую шапку со ссылкой на `evidence-8`;
- `ab53e4c0`: историческими шапками закрыты устаревшие точки входа в
  `project_docs/llm/`, hardware и weighted-hot-experts;
- `2f01a246`: в `AGENTS.md` явно помечен превзойдённый блок приоритетов, а
  `docs/parameters.md` больше не обещает автоматическую активацию необходимых
  AVX-512 расширений под MSVC.

`README.md` был исправлен ранее в `28f9d5a0`. Историческое содержимое файлов
сохранено; текущие шапки останавливают выполнение устаревших инструкций и ведут
к `FORK_WORKFLOW.md` и evidence текущей сессии.
